# Phase 2 디자인 플랜 — my-music-app

> 2026-08-23 작성 (자율진행 라운드). Phase 2의 SSOT — 디자인 결정과 그 이유를 여기 못박는다.
> Nick 리뷰(마일스톤 2.5)에서 수정될 수 있음.

## 컨셉 — "청음실 (Listening Room)"

Nick의 개인 음악 허브라는 정체성에서 출발한다. 공연장의 어두운 홀과 무대 조명(앰버)의 대비 —
**어두운 배경 위 따뜻한 포인트**가 다크 테마의 기본 정서이고, 라이트 테마는 같은 관계를
차분한 도자기빛 종이 위에서 유지한다.

**로드맵 아티팩트와 폰트 패밀리를 공유**해서 프로젝트 전체의 아이덴티티를 통일한다
(Gowun Batang / Noto Sans KR / IBM Plex Mono). 색은 앱 고유로 간다 — 로드맵은 세이지+형광green
(계기판 느낌), 앱은 앰버(청음실 조명 느낌). 같은 가족, 다른 방.

### 피해야 할 것 (AI 상투 디자인)
warm cream + terracotta, near-black + 형광 초록 단독 포인트, 보라-파랑 그라디언트 히어로,
이모지 섹션 마커, 전부 가운데 정렬. 이 목록에 걸리는 선택은 하지 않는다.

## 토큰

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| `--bg` | `#EEF0F3` (쿨 포슬린) | `#14161B` (홀 어둠) | 페이지 배경 |
| `--surface` | `#FFFFFF` | `#1C1F26` | 카드/패널 |
| `--surface-2` | `#E4E7EC` | `#252932` | 입력/보조 패널 |
| `--text` | `#1B1E26` | `#E8E7E2` | 본문 |
| `--text-muted` | `#5C6472` | `#9BA1AC` | 보조 텍스트 |
| `--border` | `#D3D8E0` | `#31363F` | 경계선 |
| `--accent` | `#B36A12` (앰버, 진한) | `#F0A93C` (앰버, 밝은) | 액션/포인트 |
| `--accent-soft` | `#F6E8D4` | `rgba(240,169,60,.14)` | 포인트 배경 |
| `--spotify` | `#1DB954` | `#1DB954` | Spotify 버튼만 (브랜드색, 다른 용도 금지) |
| intent: taste | `#4C7A4F` (모스) | `#7FB884` | "내 취향" 태그 |
| intent: explore | `#4A6B94` (슬레이트) | `#8FAFD6` | "클래식·교양" 태그 |
| intent: family | `#8F5C8F` (플럼) | `#C39BC3` | "아이돌·가족" 태그 |

색 정의는 전부 `:root` 3단 가드 패턴으로: bare `:root`(라이트 전체) →
`@media (prefers-color-scheme: dark)`의 `:root:not([data-theme="light"])` →
`:root[data-theme="dark"]`. 컴포넌트는 토큰만 참조 — 미디어쿼리 안에 색 직접 정의 금지.

## 타이포그래피

| 역할 | 폰트 | 쓰임 |
|---|---|---|
| 디스플레이 | Gowun Batang (세리프) | 앱 타이틀, 섹션 제목, 곡/아티스트명 |
| 본문 | Noto Sans KR | 메모, 설명, 버튼 |
| 데이터 | IBM Plex Mono | 날짜, 카운트, 태그, 쿼리 미리보기 |

스케일: 타이틀 1.5rem / 섹션 1.05rem / 카드 제목 1rem / 본문 0.9rem / 보조 0.78rem.
본문 줄길이는 max-width 640px 컨테이너로 자연 제한.

## 레이아웃 (모바일 퍼스트)

```
┌────────────────────────┐
│ 상단바: 타이틀 · 테마토글  │  sticky
│ 탭: 전체|내취향|클래식·교양|아이돌·가족 │  ← Q06 해결: intent 필터를 탭으로 승격
├────────────────────────┤
│ 오늘의 추천 (하이라이트 패널) │
│ 탐구 — 쿼리 빌더           │
│ 도구: 태그필터·추가·내보내기·설정 │  ← 접을 수 있게
│ 컬렉션 목록 (아트 타일 카드)  │
└────────────────────────┘
```

- **탭 = intent 필터의 승격** (Q06): 라벨을 "내 취향 / 클래식·교양 / 아이돌·가족"으로 —
  Nick의 "클래식과 아이돌 별도 탭" 요청을 별도 화면이 아니라 필터 승격으로 푼다
  (사용자 1명 전제 — 화면 분리는 과설계).
- **아트 타일**: 앨범아트 데이터가 없으므로 id 해시 → 결정적 그라디언트 + 첫 글자.
  외부 이미지 없이 음악 앱의 밀도를 만든다. 같은 엔트리는 항상 같은 색 (해시라서).
- 청취 시간이 짧은 사용자 → 스캔 우선: 추천 3개가 최상단, 액션(듣기 링크)이 카드에서 한 번에.

## 모션

- 카드: hover/press 시 미세한 translate+shadow (0.15s).
- 섹션: 최초 로드 시 위→아래 순차 fade-in 1회 (0.3s, stagger).
- `prefers-reduced-motion: reduce` → 전부 끔.
- 과한 모션 금지 — 모션이 많을수록 AI 생성 티가 남.

## 테마 토글

우상단 버튼, 순환: 시스템 → 다크 → 라이트 → 시스템. 선택은 `mma_theme` localStorage에.
시스템 상태(저장값 없음)에서는 `data-theme` 스탬프 없이 미디어쿼리만 동작.

## Phase 2 마일스톤

| # | 마일스톤 | 산출물 | 확인 | 상태 |
|---|---|---|---|---|
| 2.1 | 디자인 시스템 정의 | 이 문서 | Nick 리뷰 | ✅ 초안 |
| 2.2 | 토큰·타이포·다크/라이트 | style.css 재작성 + 테마 토글 | 두 테마 모두 대비/가독 확인 | ✅ |
| 2.3 | 레이아웃 — 상단바·탭(Q06)·아트 타일 | index.html/app.js 구조 변경 | 탭 전환·타일 색 고정성 확인 | ✅ |
| 2.4 | 모션·마감 | 트랜지션 + reduced-motion | 모션 끔 설정에서 정적으로 동작 | ✅ |
| 2.5 | Nick 리뷰 & 폰 확인 | 라이브 배포 | **Nick이 폰에서 두 테마·탭·타일 확인 — Phase 2 게이트** | ⏳ 대기 |

## 이 계획의 한계 (정직하게)

자율진행으로 잡은 초안이다. 디자인은 Nick의 학습 목표이기도 하므로, 2.5 리뷰에서
"왜 이 색인가, 왜 이 폰트인가"를 같이 되짚고 Nick의 눈으로 조정하는 단계가 본편이다.
`00_META/teaching/` 문서에 각 결정의 근거를 학습용으로 풀어뒀다.
