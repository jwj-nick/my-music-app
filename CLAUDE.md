# my-music-app — Nick 전용 취향 음악 앱

> **자기완결 워크스페이스.** 이 폴더(`C:\01_Labs\my-music-app`)에서 Claude를 실행해야 로컬 컨텍스트가 로드된다.
> SSOT는 `00_META/`. 이 파일은 **운영 포인터**(세션 규약 + 불변 원칙)만 담는다.
> 아이디어 정본: `C:\idea\2026-08-22_my-music-app-idea.md` — 요구사항 원문·경위는 거기에 있다.

## 미션
사용자 1명(Nick) 전제로 범용 서비스 흉내를 내지 않는, 운영비 0원 개인 음악 앱을 만든다. Phase 1에서 기능을 갖추고 Phase 2에서 디자인에 시간을 들여 포트폴리오급 UI를 만든다 — 결과는 공개.

## 사용자 컨텍스트
- 20년+ video codec IP RTL 엔지니어. 프론트엔드 UI/디자인은 이 프로젝트의 학습 목표 중 하나(Phase 2).
- 사용자는 Nick 1명뿐이라는 전제 — 범용 앱 패턴(다중 사용자 인증, 서버 DB 등)을 과감히 생략해도 된다.

## 새 세션 시작 ★
1. `00_META/MASTER_PLAN.md` → 현재 Phase 확인.
2. `00_META/sessions/` 최신 로그 → 지난 세션 요약과 "다음 액션" 확인.
3. `00_META/open_questions.md` 미결 항목 스캔 — 특히 Q01(재생 소스)은 Phase 1 착수 전 반드시 확정.
4. 현재 위치 + 오늘 focus 1개를 제안하고 시작한다 (제안만 하고 바로 진행하지 말 것).
5. 오늘 변경 범위를 합의한 뒤 작업 → 원본→생성물 규칙 준수.

## 불변 원칙
- **공개 범위:** 단일 public repo 직행 (2026-08-22 Nick 확정 — "전체 공개 괜찮음"). study-quiz/uvm-drill과 같은 패턴: 워크스페이스 자체가 곧 배포 repo. GitHub repo 생성·최초 push는 Phase 1 첫 배포 시점에 이 워크스페이스 세션에서 진행 (지금은 로컬 git만).
- **git 규율:** `git add -A` 금지 — 건드린 경로만 명시적 스테이징.
- **원본→생성물 규칙:** 청취 기록·태그·플레이리스트 데이터가 원본, 앱 UI 코드는 그 위에서 돌아가는 생성물. 데이터 구조가 먼저, 화면은 나중.
- **사용자 1명 전제:** 확장성·다중 테넌시를 미리 설계하지 않는다. 단순화가 정답.

## 디렉토리 구조
```
00_META/           SSOT — MASTER_PLAN·decisions·open_questions·세션 로그
10_app/            앱 소스 (Phase 1부터 실제 파일 생성 — 지금은 골격만)
.claude/skills/     세션 시작/종료 스킬
```

## 소통 워크플로우
- 세션 로그 = 라운드 누적: `00_META/sessions/YYMMDD_<주제>.md`, 끝에 "다음 액션" 1~2줄.
- 진도/단계 전환 → `MASTER_PLAN.md` · 의사결정 → `decisions.md` · 확인 필요 → `open_questions.md`.

## 답변 스타일
- 짧고 명료. 높은 수준 기술 토론. 질문은 완전한 문장으로 1~2개씩, 선택지 의미를 설명해서.
- 분석/작업 후 다음 단계 제안 또는 좋은 질문 1개.

## 로컬 스킬
| 명령 | 용도 |
|---|---|
| `/my-music-start` | 세션 시작 — 재진입 카드 + 오늘 focus 제안 |
| `/my-music-log` | 세션 종료 — 로그·MASTER_PLAN·미결질문 롤업 |
