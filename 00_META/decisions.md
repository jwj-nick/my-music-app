# 의사결정 기록 — my-music-app

> 형식: 날짜 · 결정 · 이유 · 버린 대안. 나중에 "왜 이렇게 했더라"를 여기서 복구한다.

| # | 날짜 | 결정 | 이유 | 버린 대안 |
|---|---|---|---|---|
| 1 | 2026-08-22 | 워크스페이스 스캐폴드 (lab-init), 공개 범위 = 단일 public repo 직행 | Nick이 "전체 공개 괜찮음"으로 확정 — 개인 취향 앱이지만 비공개로 시작할 이유 없음. GitHub repo 생성 자체는 Phase 1 첫 배포 시점으로 미룸 (lab-init 규율 — 스캐폴드 세션은 로컬 git만) | 비공개 로컬로 시작 후 나중에 ccar-f식으로 워크스페이스/public repo 분리 |
| 2 | 2026-08-22 | 최종 배포 형태 = 안드로이드 APK(개인 전용). Phase 1은 공개 GitHub Pages 배포로, 사실상 Nick의 sandbox로 활용 | Nick이 최종 목표를 APK로 명시. Phase 1 공개 repo는 실질적으로 비공개처럼 쓰되 결과는 공개 | 처음부터 네이티브 앱으로 시작 |
| 3 | 2026-08-22 | Q03 확정 — 폰 우선(모바일 퍼스트) | 최종 목표가 APK인 이상 자명 | 데스크톱 우선 |
| 4 | 2026-08-22 | Q01 확정 — 재생은 Spotify 무료 계정 + 딥링크(곡 링크를 열면 Spotify 앱이 재생), my-music-app은 재생기를 직접 만들지 않는다 | 로컬 음원 없음, 유료 구독 없음. Spotify 무료는 백그라운드 재생이 막혀있지 않은 반면 YouTube 무료는 백그라운드 재생이 Premium 전용이라는 차이가 결정적 (100% 확신은 아니라 open_questions Q01에 검증 필요 표시) | YouTube IFrame 임베드, 로컬 파일+Capacitor, 자체 재생기 구현 |
| 5 | 2026-08-22 | 취향 셋업 = 이 세션에서 Claude와의 대화로 취향을 정적 데이터 파일로 뽑아낸다. 앱은 런타임에 AI를 호출하지 않고 그 데이터 위에서 규칙 기반으로만 추천한다 | 운영비 0원 원칙 + 공개 repo에 API 키를 노출하는 문제를 원천적으로 피함 | 앱이 배포 후에도 실시간 LLM 호출로 추천 생성 |
| 6 | 2026-08-22 | Phase 1 앱의 정체성 = "취향 큐레이션 + 음악 지식노트 허브"(추천 레이어). 재생은 Spotify에 위임하고 자체 재생기는 만들지 않는다 | Nick: "일반 음악서비스는 사업성을 위함, 나는 내 경험·지식 확장을 위함" — 기존 학습앱 패턴(paper-study-app 등)을 음악에 적용하는 방향과 합치 | 범용 스트리밍 앱 재구현 |
| 7 | 2026-08-22 | 데이터 스키마 확정(마일스톤 1.1) — entry의 `artist`를 `credits` 객체(performer/composer/lyricist/originalArtist/conductor/ensemble)로 분리 | 작사/작곡 구분, 클래식(작곡가 vs 연주자/오케스트라), 커버곡(원곡자 vs 실연자) 세 문제가 "만든 사람 vs 들려주는 사람" 분리라는 같은 패턴이라 하나의 구조로 해결 | `artist` 단일 문자열 유지 |
| 8 | 2026-08-22 | 클래식 후보 리서치를 별도 마일스톤(1.2)으로 분리 | 스키마 검증(형태)과 실제 음반 후보 찾기(내용 수집)는 다른 성격의 작업 | 스키마 마일스톤 안에서 함께 처리 |
| 9 | 2026-08-22 | 추천 로직(1.6) 설계 원칙 — 긴 목록보다 소수의 큐레이션된 픽 | Nick: "음악 듣는 시간 길지는 않음" | 탐색형 긴 리스트 UI |
| 10 | 2026-08-22 | 클래식 후보(마일스톤 1.2) 9개 중 롯데콘서트홀 실황 1개는 seed에서 제외 | 정확한 발매연도를 출처로 확인 못함 — 데이터 정확성을 우선. 한국 음악가 비중은 2/8(25%)로 목표(30%)보다 약간 낮지만 근접 | 연도 미상인 채로 포함 |
| 11 | 2026-08-22 | "리센느"는 실제 그룹 RESCENE(2024 데뷔)로 확인 후 seed에 반영 | 검색으로 실존 확인 — 확인 없이 이름을 그대로 데이터화하지 않는다는 원칙 | 확인 없이 바로 입력 |
| 12 | 2026-08-22 | 로컬 확인은 `file://` 직접 열기 대신 로컬 서버(`python -m http.server`)로 | file://에서는 브라우저가 로컬 JSON `fetch`를 CORS로 막는다. Phase 3(TWA/PWA 패키징)도 같은 제약이라 지금 익혀둔다 | seed 데이터를 `.js` 변수로 인라인해서 CORS 우회 |
| 13 | 2026-08-22 | Spotify 딥링크 — `spotifyUrl`이 비어있으면 검색 링크(`open.spotify.com/search/...`)로 대체 | 아직 실제 트랙 URL을 확보 못함. 있으면 그걸 우선 사용하도록 설계 | 실제 URL 확보 전까지 딥링크 기능 보류 |
| 14 | 2026-08-22 | 인앱 입력(마일스톤 1.5) — seed(고정)·overrides(기존 항목 수정)·local(신규 항목) 3레이어를 런타임에 합쳐서 표시. seed.json 자체는 건드리지 않음 | seed.json은 git으로 관리되는 "출고 데이터"라 앱이 직접 덮어쓰면 안 됨. 동기화는 "내보내기" JSON을 다음 세션에서 수동 반영 | 인앱 수정이 seed.json을 직접 갱신 |
| 15 | 2026-08-22 | 클래식/아이돌 별도 탭 요청(Q06)은 Phase 2로 배정 | Nick: "나중엔" — 지금은 intent/tag 필터로 충분, 탭 같은 내비게이션 구조는 디자인 심화 단계에서 | Phase 1에서 바로 구현 |
| 16 | 2026-08-22 | 추천 로직(1.6) — `recommend(pool, queryTags)`을 DOM/localStorage에 기대지 않는 순수 함수로 설계, `module.exports`로 Node 테스트도 가능하게 함 | 회귀 테스트(같은 입력 → 항상 같은 결과)를 실제로 자동 실행하려면 순수 함수여야 함. 브라우저에서는 `<script src>` 그대로, Node에서는 `require`로 재사용 — 빌드 도구 없이 둘 다 만족 | Math.random 등으로 매번 다른 결과를 내는 "신선함" 로직 |
| 17 | 2026-08-22 | queryTags 없을 때 기본 추천 = intent(taste/explore/family)별 1개씩 "골고루" | 청취 로그(1.7)가 아직 없어 "많이 들은 것" 기준을 못 씀. 그때까지는 균형 노출이 합리적인 임시 규칙 | 무작위 추천, 혹은 taste만 우선 노출 |
| 18 | 2026-08-22 | 청취 로그(1.7) — `recommend()`는 여전히 `new Date()`를 직접 안 쓰고, 호출부(`renderRecommend`)가 "오늘 들은 id 목록"을 `excludeIds`로 계산해 넘긴다 | `recommend()`가 내부에서 현재 날짜를 읽으면 순수 함수가 아니게 되어 회귀 테스트가 날짜에 따라 달라짐 — 순수성을 유지해 테스트 가능성을 지킴 | recommend() 내부에서 직접 `new Date()`로 오늘 필터링 |
| 19 | 2026-08-22 | 마일스톤 1.8 첫 공개 배포 완료 — repo `github.com/jwj-nick/my-music-app`(public), Pages는 GitHub Actions로 `10_app/` 폴더만 배포. 라이브 `https://jwj-nick.github.io/my-music-app/` | Pages 클래식 설정(루트/`docs`)은 `10_app` 같은 임의 서브폴더를 못 골라서 Actions 배포가 필요 — uvm-drill의 "main push→GH Action 배포" 패턴과 동일 | 앱을 repo 루트로 옮기거나 `docs/`로 리네임 |
| 20 | 2026-08-22 | **Phase 1 완료 확정** — Nick이 폰에서 라이브 배포로 1.1~1.7 전부 직접 확인 | 마일스톤 1.1~1.8 전부 산출물·확인·테스트 통과 | — |
| 21 | 2026-08-23 | Phase 1.5 아키텍처 = 서버리스 백엔드 없이 브라우저가 Claude API를 직접 호출, API 키는 localStorage에만 저장 | Anthropic이 브라우저 직접 호출을 공식 지원(`dangerouslyAllowBrowser`, CORS). repo가 이미 public이어도 키를 git에 안 넣으면 안전 확보 가능. Vercel/Cloudflare 같은 새 플랫폼 계정 관리 부담도 회피 | Vercel/Cloudflare 서버리스 함수로 키를 프록시 |
| 22 | 2026-08-23 | repo는 private 전환 가능함을 확인했으나 기존 결정(#1) 유지 — public으로 계속 | 키 보호는 repo 공개 여부와 무관한 별도 축("git에 키를 절대 커밋 안 함")으로 확보되므로, public 유지 이유(학습 공개 패턴)를 바꿀 필요가 없음 | repo를 private으로 전환 |
| 23 | 2026-08-23 | Vercel MCP 인증 플로우 중단 — 당장 불필요 | #21로 서버리스 백엔드 자체가 불필요해짐. 나중에 다른 앱에서 Vercel이 필요해지면 그때 재개 | 인증을 마무리해서 미리 계정 연결 |
| 24 | 2026-08-23 | Workload Identity Federation(WIF)은 검토 후 미채택 — 정적 API 키 + spend limit 유지 | WIF는 GitHub Actions·AWS·GCP·Entra ID·Kubernetes처럼 이미 OIDC로 신원이 있는 "워크로드"(자동화 인프라)를 위한 기능이다. 브라우저에서 도는 개인 정적 사이트는 그런 워크로드 정체성이 없어 federation을 걸 IdP 자체가 없다. 쓰려면 #21에서 배제한 종류의 백엔드/클라우드 인프라를 다시 세워야 해서 목적에 안 맞음 | Anthropic Console의 Workload Identity Federation으로 정적 키 없이 인증 |
| 25 | 2026-08-23 | Claude 모델 기본값 = `claude-haiku-4-5-20251001` | 개인 앱 운영비 0원 원칙(CLAUDE.md 미션)에 맞춰 가장 저렴한 모델을 기본으로. 답변 품질이 아쉬우면 `CLAUDE_MODEL` 상수 하나만 바꾸면 됨 | 기본값을 claude-sonnet-5로 |
| 26 | 2026-08-23 | "AI에게 물어보기"(1.5.5)는 항목(카드)별 버튼으로 구현 — 질문에 그 엔트리의 title/credits를 맥락으로 붙여 전송, 응답은 "메모에 저장" 클릭 시 기존 메모 뒤에 이어붙임(덮어쓰지 않음) | 지식노트가 엔트리 단위로 누적된다는 원래 컨셉(Nick: "음악 들으면 관련 정보도 수집해서 쌓아두면 좋을듯")과 자연스럽게 맞물림. 이어붙이는 이유는 기존 메모가 사라지는 걸 막기 위함 | 전역 채팅창 하나로 통합 |
| 27 | 2026-08-23 | AI 기능 테스트는 반드시 로컬 서버나 실제 배포 사이트(GitHub Pages)에서 — 프리뷰 아티팩트(claude.ai 아티팩트)에서는 불가 | 아티팩트는 보안을 위해 강한 CSP로 외부 호스트로의 fetch/XHR을 원천 차단한다(Google Fonts 예외). Nick이 프리뷰에서 테스트했을 때 뜬 "네트워크 오류: Failed to fetch"는 Anthropic API 문제가 아니라 이 샌드박스 제약이었음 — file://에서 CORS가 막히던 것(#12)과 같은 종류의 "실행 환경이 기능을 제한하는" 케이스 | 프리뷰에서 계속 재시도, 또는 CSP를 우회하는 방법 탐색 |
| 28 | 2026-08-23 | `askClaude()`에 Anthropic 웹 검색 도구(`web_search_20250305`) 추가 — 질문당 최대 3회(`CLAUDE_MAX_SEARCHES`), `max_tokens` 512→1024로 상향, 응답에서 인용 URL을 모아 "출처" 목록으로 덧붙임 | Nick이 "웬디 최근 1년 커버영상 TOP 10"을 물었을 때 모델이 학습 데이터 컷오프 이유로 답을 거부 — 원인은 모델 품질이 아니라 도구 없이 순수 텍스트 생성만 호출한 설계였음. 웹 검색은 Anthropic이 서버에서 직접 실행하는 "server tool"이라 우리 쪽 백엔드 없이도(#21과 계속 양립) 실시간 정보 접근이 가능해짐. 검색 여부는 Claude가 질문을 보고 스스로 판단(설계 원칙 "구조는 최소로"와 부합, 우리가 분기 로직 안 짬) | 프롬프트만 다듬기(근본 해결 안 됨), 별도 검색 API를 직접 연동 |
| 29 | 2026-08-23 | **스마트 기능 재기획** — 원칙: "사용자는 목적을 고르고, 프롬프트는 앱이 조립한다". 목적 3종(①깊이=카드별 지식노트, ②발견=취향 프로파일 기반 새 음악 3개, ③트렌드=최근 화제) + 프롬프트 파이프라인(목적 템플릿 상수 + `tasteProfile()` 자동 컨텍스트 + 제외 목록 + 출력 형식 계약 + 사용자 한 줄). `tasteProfile()`·`buildPrompt()`는 순수 함수로 Node 테스트. AI 응답은 ①메모, ②③새 엔트리로 **다시 앱 데이터가 됨** | Nick 지적: 카드별 자유 질문창은 "그냥 채팅창"이지 스마트 기능이 아님 — 앱의 존재 이유는 Nick의 데이터(취향·태그·로그)를 프롬프트에 자동으로 녹여 매번 설명할 필요를 없애는 것. 원본 아이디어의 (A)정보 축적과 (B)취향 탐구·확장이 별개 요구인데 (A)만 구현되어 있었음. 출력 형식을 계약해야 응답이 파싱→엔트리화되어 "진화하는 컬렉션" 고리가 닫힘 | 자유 텍스트 채팅창 하나로 전부 커버 |
| 30 | 2026-08-23 | ②발견 입력 방식 = 3단 병행(Nick 지정): (a) pre-defined 선택 트리(취향 데이터에서 생성 — "전체 취향" / 아티스트별 계열 / 클래식 등), (b) 선택 + 짧은 자유 힌트, (c) 완전 자유 쿼리. ③트렌드 범위 = **한국 / 미국 / 세계** 3개 지역 선택 | 방향 조절의 주도권은 사용자에게 — 단 선택지는 앱이 데이터에서 만들어줘서 타이핑 부담 최소화. 완전 자유 쿼리도 남겨 유연성 확보 | 힌트 없는 자동 3개 추천만, 딸픽 앵커 고정 트렌드 |
| 31 | 2026-08-23 | **답 4유형 확정**(Nick 승인) — T1 플레이 링크(영상/음반, `곡명\|아티스트\|채널\|날짜\|URL` 목록 → 골라서 새 track 엔트리화), T2 지식(문단+출처 → 카드 메모), T3 추천 후보(`제목\|아티스트\|이유\|태그` ×N → "+추가" 엔트리화), T4 요약 브리핑(불릿+출처 → 읽고 소비, 관심 항목만 승격). 쿼리 빌더의 Step 1 = "어떤 답을 원하나"가 이 4유형 선택 | 트리(입력)보다 답(출력)의 종류가 먼저다 — Nick 지적. 유형마다 출력 계약과 **앱 데이터 행선지**가 달라서, 답 유형을 먼저 고르면 계약·저장 경로가 자동으로 따라옴. "비교" 같은 변형은 T2의 하위로 취급 | 자유 질문 하나에 형식 없는 답변 |
| 32 | 2026-08-23 | T1 결과는 **골라서 일부만 엔트리화**(Nick 지정) — 검색 결과 목록에서 각 항목 옆 "+추가"로 개별 선택, 전체 자동 저장 없음 | "추천은 짧게" 원칙(#9)과 일관 — 컬렉션은 큐레이션된 것만. 결과 전부 저장하면 정리 부담이 생김 | 결과 전부 일괄 저장 후 나중에 정리 |
| 33 | 2026-08-23 | 스키마 확장 — entry에 선택 필드 `links: [{label, url}]` 추가 (하위호환, 기존 엔트리 무변경). T1로 찾은 영상 링크가 여기 저장됨 | 지금 엔트리엔 `spotifyUrl` 하나뿐인데 T1의 결과는 대부분 YouTube 영상 — seed의 웬디 자리표시자("곡별로 엔트리 여러 개 생길 것")가 예약해둔 자리를 이 필드로 채움. 원본→생성물 규칙: 데이터 구조 먼저 | 엔트리당 URL 1개 유지(spotifyUrl에 유튜브 링크 혼용) |
| 34 | 2026-08-23 | Phase 2 디자인 = "청음실" 컨셉 (SSOT: `design_plan.md`) — 앰버 액센트(무대 조명), 쿨 포슬린/홀 어둠 배경, 폰트는 로드맵 아티팩트와 통일(Gowun Batang/Noto Sans KR/IBM Plex Mono), intent 3색(모스/슬레이트/플럼), Spotify green은 브랜드 버튼 전용 | 개인 음악 허브의 정서 + 프로젝트 전체 아이덴티티 통일(같은 폰트 가족, 색은 방마다 다름). AI 상투 조합(warm cream+terracotta, 보라-파랑 그라디언트 등) 회피를 명시적 제약으로 | 로드맵과 동일한 세이지+형광green 재사용 |
| 35 | 2026-08-23 | Q06 해결 방식 = intent 필터를 상단 탭으로 승격 (전체/내 취향/클래식·교양/아이돌·가족) — 별도 화면 분리 안 함 | 사용자 1명 전제에서 화면 분리는 과설계. 탭 라벨을 Nick의 실제 용어(클래식, 아이돌)로 바꾸는 것으로 요청의 본질(한눈에 구분)을 충족 — 2.5 리뷰에서 판정 | intent별 독립 화면/라우팅 |
| 36 | 2026-08-23 | 아트 타일 = 엔트리 id 해시 → 결정적 HSL 그라디언트 + 제목 첫 글자 | 앨범아트 데이터가 없고 외부 이미지는 관리 비용 — 해시 기반이라 같은 엔트리는 영원히 같은 색(재현성, recommend() 순수성과 같은 철학) | 외부 앨범아트 API 연동(비용·의존성), 이미지 없음 |
| 37 | 2026-08-23 | 프리뷰 아티팩트를 실제 10_app 파일에서 **스크립트로 자동 생성**(`scratchpad/build_preview.js`) — fetch를 인라인 데이터로 치환해 조립 | 수동 포팅은 라운드마다 실수 여지(이미 두 번 동기화 어긋날 뻔). 실제 코드가 곧 프리뷰가 되도록 | 계속 수동 미러링 |
